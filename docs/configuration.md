# Configuration

The configuration API is not implemented in v0.1.0. The physical proof accepts only non-sensitive
command-line timing values and the expected advertised device name. It neither accepts nor stores a
Bluetooth address or any secret.

