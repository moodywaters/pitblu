# REST API

The REST API is not implemented in v0.1.0. It begins in v0.3.0 after the physical BLE proof and
device-foundation milestone have passed. The future contract is versioned under `/api/v1`; only
minimal `/health` and authenticated `/ready` sit outside that prefix.

